"""F-01 수집 서비스 묶음 — 뉴스(news) · DART 후속공시(filings) · 보고서 PDF(reports) · KRX(옵션 경로).

pipeline_runs 기록 헬퍼(start_run · finish_run · decide_status · TRIGGER)는 services/runs.py 에 있다 — 여기서 재export 하지 않는다.
"""
